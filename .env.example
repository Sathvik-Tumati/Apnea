# Vital Signs ML Pipeline Environment Variables

# Backend Database Path (defaults to vitals_pipeline.db in the project root)
# DB_PATH=vitals_pipeline.db

# FastAPI CORS Origins (comma separated, defaults to *)
# CORS_ORIGINS=http://localhost:5174

# Optional: Add any secret keys or external API URLs here in the future
